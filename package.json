{
  "name": "onion-hono-sample",
  "scripts": {
    "dev": "bun run --hot src/Cmd/index.ts",
    "migrate": "bun run src/Infra/Database/migrate.ts",
    "test": "bunx --bun vitest",
    "test:ui": "bunx --bun vitest --ui",
    "install:lib": "docker compose run --rm api bun install && bun install"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.6.1",
    "@types/bcrypt": "^6.0.0",
    "bcrypt": "^6.0.0",
    "config": "^4.4.1",
    "hono": "^4.12.25",
    "js-yaml": "^5.2.0",
    "kysely": "^0.29.2",
    "pg": "^8.21.0",
    "tsyringe": "^4.10.0",
    "uuidv7": "^1.2.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.20.0",
    "typescript": "^5.9.2"
  }
}
